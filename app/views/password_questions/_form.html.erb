<%= form_for(@password_question) do |f| %>
  <% if @password_question.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@password_question.errors.count, "error") %> prohibited this password_question from being saved:</h2>

      <ul>
      <% @password_question.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
